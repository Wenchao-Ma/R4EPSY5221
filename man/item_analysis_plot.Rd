% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/itemplot.R
\name{item_analysis_plot}
\alias{item_analysis_plot}
\title{Create Item Analysis Scatter Plot}
\usage{
item_analysis_plot(
  data,
  difficulty_range = c(30, 70),
  min_discrimination = 0.3,
  item_labels = NULL,
  title = "Item Analysis Plot"
)
}
\arguments{
\item{data}{A data frame or matrix where rows are students and columns are items.
Each cell should contain 0 (incorrect) or 1 (correct).}

\item{difficulty_range}{A numeric vector of length 2 specifying the acceptable
difficulty range. Default is c(30, 70).}

\item{min_discrimination}{A numeric value specifying the minimum acceptable
discriminability. Default is 0.3.}

\item{item_labels}{A character vector of item labels. If NULL, uses column names
or numbers 1:ncol(data).}

\item{title}{A character string for the plot title. Default is "Item Analysis Plot".}
}
\value{
A ggplot object. Also invisibly returns a data frame with item statistics.
}
\description{
This function creates a scatter plot showing the relationship between
item discriminability (point-biserial correlation) and item difficulty
(percent passing) for binary item response data.
}
\examples{
# Generate sample data
set.seed(123)
sample_data <- matrix(rbinom(500 * 10, 1, 0.6), nrow = 500, ncol = 10)
colnames(sample_data) <- paste0("Item_", 1:10)

# Create plot
item_analysis_plot(sample_data)

}
